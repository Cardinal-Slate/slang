# Changelog

## [Unreleased]

- Initial cut: the type layer. A type is a structure; `slang_has` and `slang_fits` are `slate_order`
  (level = the same type); `slang_derive` is `slate_compose` over a function type. No schema, no slots —
  the spine's three ops are the whole checker. Point/Vector demonstrate typing, fit, and composition.
