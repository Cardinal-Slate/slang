# Changelog

## [Unreleased]

- Initial cut: the type layer. A type is a structure; `CSE-Types_has` and `CSE-Types_fits` are `slate_order`
  (level = the same type); `CSE-Types_derive` is `slate_compose` over a function type. No schema, no slots —
  the spine's three ops are the whole checker. Point/Vector demonstrate typing, fit, and composition.
